request=require('request')
  request({
    method: 'GET',
    uri: 'https://localhost/ip',
    rejectUnauthorized: false
  },
  function (error, response, body) {
    if (error) {
      return console.error('upload failed:', error);
    }
    console.log('Server responded with: ' + body);
  })
