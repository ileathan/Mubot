(function() {
  var SlackBot;

  SlackBot = require('./src/bot');

  exports.use = function(bot) {
    return new SlackBot(bot, {
      token: process.env.MUBOT_SLACK_TOKEN
    });
  };

}).call(this);
