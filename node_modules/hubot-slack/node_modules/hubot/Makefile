NPM_EXECUTABLE_HOME := node_modules/.bin

PATH := ${NPM_EXECUTABLE_HOME}:${PATH}

test: deps
	@script/test

dev: generate-js
	@coffee -wc --bare -o lib src/*.coffee

generate-js:
	@find src -name '*.coffee' | xargs coffee -c -o lib

package:
	@bin/imubot -c imubot
	@chmod 0755 imubot/bin/imubot

remove-js:
	@rm -fr lib/

deps:

.PHONY: all

