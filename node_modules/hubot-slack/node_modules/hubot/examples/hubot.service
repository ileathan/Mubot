; Hubot systemd service unit file
; Place in e.g. `/etc/systemd/system/imubot.service`, then `systemctl daemon-reload` and `service imubot start`.

[Unit]
Description=Hubot
Requires=network.target
After=network.target

[Service]
Type=simple
WorkingDirectory=/path/to/imubot
User=change-to-imubot-user

Restart=always
RestartSec=10

; Configure Hubot environment variables, make sure to use quotes around whitespace as shown below.
Environment=MUBOT_aaa=xxx "MUBOT_bbb='yyy yyy'"

ExecStart=/path/to/imubot/bin/imubot --adapter zzz

[Install]
WantedBy=multi-user.target
