; Hubot systemd service unit file
; Place in e.g. `/etc/systemd/system/mubot.service`, then `systemctl daemon-reload` and `service mubot start`.

[Unit]
Description=Hubot
Requires=network.target
After=network.target

[Service]
Type=simple
WorkingDirectory=/path/to/mubot
User=change-to-mubot-user

Restart=always
RestartSec=10

; Configure Hubot environment variables, make sure to use quotes around whitespace as shown below.
Environment=MUBOT_aaa=xxx "MUBOT_bbb='yyy yyy'"

ExecStart=/path/to/mubot/bin/mubot --adapter zzz

[Install]
WantedBy=multi-user.target
