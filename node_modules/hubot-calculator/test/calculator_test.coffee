chai = require 'chai'
sinon = require 'sinon'
chai.use require 'sinon-chai'

expect = chai.expect

describe 'calculator', ->
  beforeEach ->
    @bot =
      respond: sinon.spy()
      hear: sinon.spy()

    require('../src/calculator')(@bot)

  it 'registers a respond listener for calculating', ->
    expect(@bot.respond).to.have.been.calledOnce
