# Description:
#   Stores the brain in Postgres
#
# Dependencies:
#   "pg": "~0.10.2"
#
# Configuration:
#   DATABASE_URL
#
# Commands:
#   None
#
# Notes:
#   Run the following SQL to setup the table and column for storage.
#
#   CREATE TABLE mubot (
#     id CHARACTER VARYING(1024) NOT NULL,
#     storage TEXT,
#     CONSTRAINT mubot_pkey PRIMARY KEY (id)
#   )
#   INSERT INTO mubot VALUES(1, NULL)
#
# Author:
#   danthompson

Postgres = require 'pg'

# sets up hooks to persist the brain into postgres.
module.exports = (bot) ->

  database_url = process.env.DATABASE_URL

  if !database_url?
    throw new Error('pg-brain requires a DATABASE_URL to be set.')

  client = new Postgres.Client(database_url)
  client.connect()
  bot.logger.debug "pg-brain connected to #{database_url}."

  query = client.query("SELECT storage FROM mubot LIMIT 1")
  query.on 'row', (row) ->
    if row['storage']?
      bot.brain.mergeData JSON.parse(row['storage'].toString())
      bot.logger.debug "pg-brain loaded."

  client.on "error", (err) ->
    bot.logger.error err

  bot.brain.on 'save', (data) ->
    query = client.query("UPDATE mubot SET storage = $1", [JSON.stringify(data)])
    bot.logger.debug "pg-brain saved."

  bot.brain.on 'close', ->
    client.end()

