# Description:
#  Accepts alert POSTs from logentries at `<imubot server>/imubot/logentries`
#  and broadcasts the alert name in a specified room.
#
#  Example broadcast for an Exit timeout alert:
#
#    "[<Your Logentries App Name> Logs]: Exit timeout"
#
# Dependencies:
#   None
#
# Configuration:
#   LOGENTRIES_ROOM
#     room number where imubot will say what alerts
#     were received from logentries
#
# Commands:
#   None
#
# URLs:
#   POST /imubot/logentries
#
# Author:
#   eprothro
#

# https://logentries.com/doc/webhookalert/
# The body.payload contains the following JSON string:
#
#        {
#            "alert": {
#                "name": "Logentries Alert Name"
#            },
#            "host": {
#                "name": "Host name",
#                "hostname": "web.example.com"
#            },
#            "log": {"name": "Logentries application name"},
#            "event": <triggering event information object>,
#            "context": [<context event information objects>]
#        }

# TODO: move rooms into request path: imubot/<room-numbers>/logentries/
#       as to not require the ENV var.
#
# TODO: add link to go to logentries log from broadcast

module.exports = (bot) ->
  bot.router.post "/imubot/logentries", (req, res) ->

    if req.body.payload
      data = JSON.parse req.body.payload
      alert = data.alert.name
      name = data.log.name

      bot.logger.info "Alert POSTed from LogEntries: #{alert}"

      user = bot.brain.userForId 'broadcast'
      if process.env.LOGENTRIES_ROOM
        user.room = process.env.LOGENTRIES_ROOM
      user.type = 'groupchat'

      bot.send user, "[#{name} #{alert} #{data.host.name}]: #{data.event.m}"

      res.writeHead 200, {'Content-Type': 'text/plain'}
      res.end 'Thanks\n'
    else
      bot.logger.error "Logentries: unexpected message format"
      bot.logger.info JSON.stringify(req.body)
      res.writeHead 422, {'Content-Type': 'text/plain'}
      res.end 'Expected payload JSON string to exist\n'
