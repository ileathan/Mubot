# Description:
#   Show ReplyGifs based on tags. See http://replygif.net.
#
# Dependencies:
#   None
#
# Configuration:
#   MUBOT_REPLYGIF_API_KEY: the api key for replygif.net, defaults to public key "39YAprx5Yi"
#
# Commands:
#   mubot replygif <tag> - Embed a random ReplyGif with the given tag.
#   mubot replygif me <tag> - Same as `mubot replygif <tag>`.
#   mubot replygif id <id> - Embed the ReplyGif with the given id
#   mubot replygif me id <id> - Same as `mubot replygif id <id>`.
#
# Notes:
#   Use 'rg' as shorthand for the 'replygif' command
#
# Author:
#   altschuler (previous non-api version by sumeetjain, meatballhat)

apiKey = process.env.MUBOT_REPLYGIF_API_KEY or "39YAprx5Yi"

apiUrl = "http://replygif.net/api/gifs?api-key=#{apiKey}"

module.exports = (bot) ->
    apiCall = (msg, failMsg, query) ->
        bot.http(apiUrl + query).get() (err, res, body) ->
            try
                gifs = JSON.parse body
            if not gifs? or not gifs.length
                msg.send failMsg
            else
                msg.send (msg.random gifs).file

    bot.hear /.*replygif\.net\/(i\/)?(\d+)(?!.*\.gif).*/i, (msg) ->
        id = msg.match[2]
        msg.send "http://replygif.net/i/#{id}.gif"

    bot.respond /(replygif|rg)( me)? ([\w|\ ]+)/i, (msg) ->
        tag = msg.match[3]
        if tag is "id" then return # mubot's looking for an id
        apiCall msg, "I don't know that reaction", "&tag=#{tag}"

    bot.respond /(replygif|rg)( me)? id (\d+)/i, (msg) ->
        id = msg.match[3]
        apiCall msg, "I don't any gifs with that id", "&id=#{id}"
