# Description:
#   Return random animated GIFs from giftv
#
# Dependencies:
#   None
#
# Configuration:
#   None
#
# Commands:
#   mubot giftv me - Returns a random animated GIF
#
# Author:
#   brettbuddin

module.exports = (bot) ->
  bot.respond /giftv( me)?$/i, (msg) ->
    msg
      .http('http://www.gif.tv/gifs/get.php')
      .get() (err, res, body) ->
        msg.send 'http://www.gif.tv/gifs/' + body + '.gif' || 'Could not compute.'
